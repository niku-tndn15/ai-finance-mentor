[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  # Pin Node to match local (Next 14.2 needs >=18.17; 20 is current LTS).
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
